[
  {kernel, [
    {error_logger, silent},
    {logger_level, warning},
    {logger, [
      {handler, default, logger_std_h, #{
        config => #{
          type => standard_error
        }
      }}
    ]}
  ]},
  {sasl, [
    {sasl_error_logger, false}
  ]}
].
